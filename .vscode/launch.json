{
    "version": "0.2.0",
    "configurations": [
    
        {
            "name": "Launch file",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/backend/cmd/server/main.go",
            "cwd": "${workspaceFolder}/backend/cmd/server" // Ensure this points to the correct directory
        }
    ]
}